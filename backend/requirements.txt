fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
sqlalchemy>=2.0,<3.0
python-dotenv>=1.0,<2.0
httpx>=0.27,<1.0
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0

# LLM local (LocalLLMProvider) -- sempre instalado (wheel pre-compilada
# no PyPI, sem custo de build) para nao depender de um passo de setup
# separado alem de "pip install -r requirements.txt". So e usado se
# LLM_PROVIDER=local no .env; nao afeta quem usa stub/anthropic.
gpt4all>=2.8,<3.0
